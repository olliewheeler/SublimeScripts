name: "Test Rule - Keyword Trigger"
description: "Triggers on a known test string"
type: "rule"

source: |
  strings.icontains(subject.subject, "SUSPICIOUS_TEST_STRING_123")
  or strings.icontains(body.plain.raw, "SUSPICIOUS_TEST_STRING_123")
